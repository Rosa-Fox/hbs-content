require 'spec_helper'

describe Hbs::Content do
  it 'has a version number' do
    expect(Hbs::Content::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
